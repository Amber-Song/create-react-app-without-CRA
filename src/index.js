/**
 * This is the entry point for webpack to process
*/

import React from "react";
import ReactDOM from "react-dom";

import App from "./app.jsx";
ReactDOM.render(<App />, document.getElementById("root"));